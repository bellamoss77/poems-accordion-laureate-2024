import { useState } from 'react';
import Lightbox from './Lightbox';

const ImageGallery = () => {
    const [selectedImage, setSelectedImage] = useState(null);
    
    const images = [
        {
            src: './images_gallery/Catherine_Selfridge-square.png',
            
        }
    ]
}